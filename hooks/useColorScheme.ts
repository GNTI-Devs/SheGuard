/**
 * useColorScheme — reads from ThemeContext (supports in-app light/dark override).
 * Falls back to system scheme when ThemeProvider is not mounted.
 */
export { useColorScheme } from 'react-native';
